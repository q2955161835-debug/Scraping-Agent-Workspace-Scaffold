# 项目地图

## 目标
- 整合 `agent-reach`（代理触达项目）到当前数据爬取工作区。
- 当前阶段只完成工作区接纳目录，原始项目内容尚未导入。
- 后续若包含网页采集、数据采集或外部来源整理，必须继续以 `Scrapling`（网页抓取库）为核心采集准则。

## 目录
- `doc/`：项目规则、进展记录、目录说明、变量表说明、采集策略。
- `旧数据/`：历史版本数据。
- `原始数据/`：原始结构化抓取结果。
- `原始文件/`：下载文件、网页快照、报告、截图等。
- `try/`：测试、调试、临时验证文件。

## 扩展目录
- 暂无扩展目录。

## 待导入内容
- 原始 `agent-reach`（代理触达项目）代码、文档或数据源目录尚未定位。
- 导入前需要确认来源路径，并按工作区目录分类放置到 `脚本/`、`原始数据/`、`原始文件/` 或其他经文档说明的扩展目录。
