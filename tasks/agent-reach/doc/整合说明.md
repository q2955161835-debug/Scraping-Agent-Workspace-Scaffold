# agent-reach 整合说明

## 当前状态
- 已在工作区根目录创建 `agent-reach/` 标准子项目目录。
- 原始 `agent-reach`（代理触达项目）目录尚未在当前工作区、`D:\1Folder\爬虫work`、`D:\1Folder`、`D:\2Folder` 或 `D:\` 第一层定位到。

## 整合原则
- 不改变 `Scrapling`（网页抓取库）作为网页采集核心的准则。
- 若原项目存在其他采集方式，需要作为辅助能力接入，并在 `doc/采集策略.md` 说明适用场景、失败信号和与 `Scrapling`（网页抓取库）的关系。
- 导入文件时按目录职责分类，不把代码、数据或临时文件散落在子项目根目录。

## 下一步
- 确认原始 `agent-reach`（代理触达项目）的实际路径。
- 读取原项目结构后，再决定是否新增 `脚本/`、`配置/`、`中间结果/` 等扩展目录。
