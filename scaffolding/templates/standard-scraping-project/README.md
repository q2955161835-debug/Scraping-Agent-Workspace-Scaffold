# 标准爬取子项目模板

复制或使用脚本生成本模板后，先填写 `doc/变量表说明.md` 和变量确认表，再开始采集。
正式采集前同步维护 `doc/任务清单.md`，登记目标 URL（统一资源定位符）或文件、去重键、任务状态和输出位置。

## 默认目录

- `doc/`：项目规则、进展、变量说明、采集策略。
- `doc/任务清单.md`：爬取任务、去重键、状态、输出位置和失败项。
- `旧数据/`：历史版本。
- `原始数据/`：原始结构化抓取结果。
- `原始文件/`：网页快照、附件、截图、报告。
- `try/`：测试和临时验证文件。
