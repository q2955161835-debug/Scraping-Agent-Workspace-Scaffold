# 数据爬取工作区 Agent 规则

## 回复与协作
- 默认使用中文回复；必须出现英文术语时，在英文内容后加括号说明中文，例如 `Scrapling`（网页抓取库）。
- 每次最终回复开头称呼用户 `boss`。
- 当前根目录是工作区本体，目录名为 `数据爬取 Agent 工作区脚手架`。
- `脚手架/` 是根目录下的独立脚手架能力目录，保存可复用模板、脚本、示例和 `workspace-scraping`（工作区爬取）技能。
- 工作区包裹脚手架：目标数据子项目放在根目录第一层，复用能力从 `脚手架/` 调用。
- 禁止制定重复规则。新增规则前先检查 `AGENTS.md`、`doc/项目地图.md`、`脚手架/tools/工具库说明.md`、`脚手架/tools/workspace-scraping/SKILL.md` 和子项目 `doc/`；已有规则只引用，不换一种说法重写。
- 同一规则只保留一个主位置；其他文件只写入口、索引、差异化补充或执行细则。

## 工作区目标
- 本工作区专门负责数据爬取、数据整理、质量分级、变量表确认和最终数据交付。
- 工作重点是提高多渠道采集效率、减少重复探索、保证数据来源和字段口径可追踪。
- `Scrapling`（网页抓取库）仍是网页采集核心准则；整合其他项目时不得改成绕开 `Scrapling`（网页抓取库）的主流程。

## 规则归属
- `AGENTS.md`：工作区级强制规则、优先级、总流程和禁止事项。
- `doc/项目地图.md`：长期索引、目录职责、工具清单、常用命令和环境概况。
- `脚手架/tools/工具库说明.md`：脚手架工具库目录、技能目录、脚本和工具维护规则。
- `脚手架/tools/workspace-scraping/SKILL.md`：本工作区执行技能细则，包括子项目创建、变量确认、渠道派遣和验收流程。
- 子项目 `doc/`：该子项目的字段口径、目录扩展、采集策略、进展和例外说明。
- `脚手架/AGENTS.md`：脚手架自身模板规则，供发布或复制脚手架时使用；当前工作区执行时以根目录 `AGENTS.md` 为准。
- 若发现重复规则，保留优先级更高或更贴近职责的主文件内容，其他位置改为引用。

## Git 与备份
- 若工作区根目录尚未创建 `Git`（版本控制）仓库，先在当前项目根目录初始化 `Git`（版本控制），并完成一次基线提交后再进行较大改动。
- 在大规模修改、批量生成、批量删除、结构性重构前，必须先检查本次操作涉及的目录是否已经处于 `Git`（版本控制）管理下；若已处于 `Git`（版本控制）管理下，先提交一次“变更前检查点”提交，再开始修改。
- 除 `Git`（版本控制）以外，高危行为前进行的备份统一保存在 `D:\0文件夹\备份` 的子文件夹，文件夹命名需要清晰，并包含时间信息。

## 进展文档
- 每完成一个阶段性任务后，更新 `doc/进展记录.md`。
- 普通问答、单纯网络搜索、未修改文件的探索不需要写入进展记录。
- 高风险操作前必须更新进展记录，并明确标记“高风险”。
- 每次记录必须写清时间段，精确到分钟，格式为 `YYYY-MM-DD HH:mm ~ YYYY-MM-DD HH:mm`，以本地时间为准。
- 记录内容保持简洁，至少包含本阶段完成内容、新增/修改/生成文件清单与用途说明。
- 若出现错误或异常，必须写入“错误汇报”。

## 项目地图与环境账本
- 工作区维护 `doc/项目地图.md`，记录长期维护信息：工作区目标、目录职责、核心入口、常用命令、关键依赖、主要功能、数据流、运行方式。
- 每个子项目可维护自己的 `doc/项目地图.md` 或 `doc/目录说明.md`，记录该子项目目录职责、字段口径、数据流和交付格式。
- 功能变更时，同步更新工作区或子项目的功能清单。
- 新增、删除或改名环境变量时，同步更新 `.env.example`，并检查代码读取逻辑、启动文档和部署配置。
- 禁止将真实密钥、`token`（令牌）、`cookie`（会话凭据）、数据库密码、私有地址写入 `.env.example`、说明文档、进展记录或聊天可复制代码块。

## 脚手架工具库
- 根目录 `脚手架/` 是独立脚手架目录，不是具体爬取子项目。
- `脚手架/tools/` 存放常用技能实体目录、可复用脚本和工作区专用技能。
- 技能目录、脚本和专用技能的维护细则写在 `脚手架/tools/工具库说明.md`，本文件不重复展开。
- 新增工具、脚本或技能目录后，同步更新 `脚手架/tools/工具库说明.md`；若影响长期索引，再更新 `doc/项目地图.md`。

## 目录分类规则
- 第一层目录按爬取目标或整合项目命名，必须是独立子项目，例如 `agent-reach/`、`某平台商品数据/`、`某城市招投标数据/`。
- 工作区根目录只放工作区级文件、工作区级 `doc/`、`脚手架/`、必要配置、`try/` 和各目标子项目，禁止把中间数据散落在根目录。
- 最新版数据放在对应子项目根目录，优先使用清晰命名，例如 `latest.xlsx`、`latest.csv`、`latest.jsonl`，也可使用更明确的 `目标名_最新版_YYYYMMDD.xlsx`。
- 第二层目录采用“默认分类 + 按需扩展”：
  - 默认建议包含 `doc/`、`旧数据/`、`原始数据/`、`原始文件/`、`try/`。
  - 可根据目标新增分类，例如 `清洗数据/`、`脚本/`、`截图证据/`、`中间结果/`、`配置/`、`导出报告/`。
  - 新增目录必须使用语义明确的中文或英文名称，同一子项目内尽量保持同一语言风格。
  - 新增目录必须在该子项目 `doc/项目地图.md` 或 `doc/目录说明.md` 中写明用途、可删除性、是否承载最终数据。
- `try/` 只放测试、调试、临时验证文件，应保证清空后不影响项目结果。

## 子项目规则读写权
- 每个子项目可在自己的 `doc/` 内维护 `项目规则.md`、`变量表说明.md`、`目录说明.md`、`采集策略.md`。
- 子项目规则可以细化字段、来源、目录、脚本、校验方式、交付格式和复核口径。
- 子项目规则不得覆盖工作区级总流程、数据质量分级和变量确认门槛。
- 当子项目规则与工作区规则冲突时，优先遵守工作区级规则，并在该子项目进展记录中说明冲突点和处理方式。

## 数据质量分级
- 数据来源质量按以下顺序分级：官方 > 官方新闻 > 第三方新闻 > 论文 > 各种渠道逆向计算出的数据 > 论坛 > 其他渠道。
- 同一变量存在多个来源时，优先采用质量等级更高的来源。
- 低等级来源可作为线索、补充或异常排查依据，但需要在数据表中标明来源等级和核验状态。
- 逆向计算数据必须记录公式、输入来源、计算口径、时间范围和误差风险。

## 爬取前变量建模
- 数据爬取的第一步必须先写清变量名，建立变量确认表。
- 变量确认表至少包含：变量名、字段含义、单位、数据类型、来源优先级、是否必填、清洗规则、可接受缺失条件、备注。
- 先生成未填入数据的初始表格，放入子项目根目录或子项目 `doc/`，交由用户确认。
- 用户确认变量表后，才进入爬虫、抓取、逆向计算或批量下载阶段。
- 若用户临时新增变量，应先更新变量确认表，再继续采集。

## 子 Agent 分工规则
- 根据目标渠道和数据质量等级派遣子 `agent`（代理），优先并行处理互不依赖的渠道，提高效率。
- 官方源子 `agent`（代理）：负责官网、公告、开放接口、官方数据库。
- 官方新闻子 `agent`（代理）：负责新闻稿、官方动态、官方媒体账号。
- 第三方新闻子 `agent`（代理）：负责行业媒体、新闻站点、转载源追踪。
- 文献提取子 `agent`（代理）：负责论文、报告、`PDF`（便携式文档格式）、白皮书。
- 逆向计算子 `agent`（代理）：负责按公开口径、公式、时间序列、同比环比推算缺失数据。
- 弱来源核验子 `agent`（代理）：负责论坛、社区、问答站、非正式渠道，作为低优先级补充。
- 汇总子 `agent`（代理）：负责合并各渠道结果、去重、质量分级、字段对齐和最终验收。
- 子 `agent`（代理）交付内容必须包含：采集范围、字段结果、来源链接或文件、质量等级、失败项、后续建议。

## 核心 Skills 工作流
- 本工作区任务优先阅读 `脚手架/tools/workspace-scraping/SKILL.md`，再按需读取 `脚手架/tools/skills/` 中的实体技能目录。
- 额外技能路由读取 `external-skills-hub`（外部技能路由），并选择最小必要技能。
- `Scrapling`（网页抓取库）、`Excel`（表格）、`mcporter`（模型上下文协议调用器）等工具的具体使用细则由 `脚手架/tools/workspace-scraping/SKILL.md` 和对应技能文件承载，本文件只保留总入口。

## 数据管理流程
- 数据文件位置按“目录分类规则”执行，不在本节重复列目录清单。
- 最终数据表应包含来源字段、质量等级字段、采集时间字段和核验状态字段。

## 验收规则
- 详细验收清单由 `脚手架/tools/workspace-scraping/references/dataset-acceptance.md` 承载。
- 工作区级最低验收：变量表已确认、字段来源可追踪、质量等级已标注、最新版数据位置符合目录分类规则。
